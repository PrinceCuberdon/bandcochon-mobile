import { Injectable } from "@angular/core";
import { Observable } from "rxjs/Observable";

export interface Localization {

}

@Injectable()
export class GoogleMapLocalization {
    constructor() {
    }

    getLocation(lat: number, lng: number) : Observable<Localization> {
    )


}
